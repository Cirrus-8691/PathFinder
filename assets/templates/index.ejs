<html>
    <head>
        <%- include('./components/head'); %>
    </head>
    <body>

        <header>
            <%- include('./components/header', {
                    href : "./findPath?from=0&to=4&by=fast", 
                    trail:"Find path from 0 to 4 by fast"} ); %>
        </header>

        <div class="layout" >
            <br>
            In graph :
            <%- include('./components/graphCanvas', { graphAscii, graphCanvas }); %>
            <br>
            Maximum (<%= maxVertexId %>) and minimum(<%= minVertexId %>) 
            are border values for vertex id.</br>
            <form action="findPath" method="get" class="form-example">
                Test : Find a path 

                <label for="from">form vertex id</label>
                <input type="number" id="from" name="from" required
                    min="<%= minVertexId %>" max="<%= maxVertexId %>" >

                <label for="to">to id</label>
                <input type="number" id="to" name="to" required
                    min="<%= minVertexId %>" max="<%= maxVertexId %>" >

                <label for="to">by (fast, eco)</label>
                <input type="text" id="by" name="by" required 
                    value="fast" size="4">

                <button type="submit">
                    Go
                </button>
            </form>
        </div>

        <footer>
            <%- include('./components/footer'); %>
        </footer>
    </body>
</html>